# 子プロジェクト1 (例: Node.jsプロジェクト)
FROM node:18-bullseye

# ワーキングディレクトリの設定
WORKDIR /workspace/subproject1

# 依存関係のインストール (必要に応じて)
# COPY subproject1/package.json ./
# RUN npm install

# 確認用メッセージ
RUN echo "Subproject1 (Node.js) container's Dockerfile."
